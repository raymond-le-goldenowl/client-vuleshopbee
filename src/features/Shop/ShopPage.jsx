import {Container, Typography} from '@mui/material';
import React from 'react';

export default function ShopPage() {
	return (
		<Container maxWidth='lg'>
			<Typography variant='h1'>ShopPage</Typography>
		</Container>
	);
}
